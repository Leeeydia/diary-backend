<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.diary.backend.aireply.AiReplyMapper">

    <resultMap id="AiReplyResultMap" type="com.diary.backend.aireply.AiReplyVO">
        <id property="id" column="id"/>
        <result property="diaryId" column="diary_id"/>
        <result property="reply" column="reply"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.diary.backend.aireply.AiReplyVO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_reply (diary_id, reply)
        VALUES (#{diaryId}, #{reply})
    </insert>

    <select id="findByDiaryId" resultMap="AiReplyResultMap">
        SELECT * FROM ai_reply WHERE diary_id = #{diaryId}
    </select>

    <delete id="deleteByDiaryId">
        DELETE FROM ai_reply WHERE diary_id = #{diaryId}
    </delete>

</mapper>
